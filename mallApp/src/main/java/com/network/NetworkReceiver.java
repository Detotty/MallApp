package com.network;


//import com.android.sip12.service.SipService;
import android.content.BroadcastReceiver;
import android.content.Context;
import android.content.Intent;

/**
 * 
 * @author Muhammad Babar Naveed
 *    Date Feb 26, 2013
 *
 */
public class NetworkReceiver extends BroadcastReceiver{
	@Override
	public void onReceive(Context context, Intent intent) {
//		if(PreferenceUtils.isLoggedIn(context) && (NetworkUtility.isWifiConnected(context) 
//				|| NetworkUtility.isHighSpeedMobileNetworkAvailable(context))){	
//			context.startService(new Intent(context, CallService.class));
//		}else if(PreferenceUtils.isLoggedIn(context)){
//			context.sendBroadcast(new Intent(HomeActivity.NO_CONNECTIVITY_ACTION));
//		}
		
	}
}
